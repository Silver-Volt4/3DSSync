# This must always echo your device's IP address and the port ftpd is running on.
# You can hardcode it or script it to return a different IP address on different WiFi access points.
echo 192.168.0.1:5000